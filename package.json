{
  "name": "react-input-price",
  "version": "0.0.2-alpha.2",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "dependencies": {
    "@juandardilag/value-objects": "^1.3.1"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^19.0.4",
    "@types/react-dom": "^19.0.2",
    "cra-template-typescript": "1.2.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "test": "echo \"Error: no test specified\"",
    "eject": "react-scripts eject",
    "prepublish": "npx rimraf dist/ && tsc",
    "pub": "npm i && git add -A && git commit --amend --no-edit && npm publish --access public && git push",
    "pub-alpha": "npm i && git add -A && git commit --amend --no-edit && npm publish --access public --tag alpha && git push"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
